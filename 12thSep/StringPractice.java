public class StringPractice {
    // Some sample string questions

    public static void main (String[] args) {
        // find out if the character 'a' is in the string "hello" using the indexOf method
        System.out.println("hello".indexOf('a') != -1);

        // find out if the character 'a' is in the string "hello" using the contains method
        System.out.println("hello".contains("a"));

        // find out if the string "hello" is in the string "aha hello world" using the indexOf method
        System.out.println("aha hello world".indexOf("hello") != -1);

        // find out if the string "hello" is in the string "hello world" using the contains method

        // find out if the string "hello" is in the string "hello world" using the substring method (and the equals method)

        // find out if the string "hello" is in the string "hello world" using the charAt method and a loop
    }
}
